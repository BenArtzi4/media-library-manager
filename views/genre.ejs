<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= song %> by <%= artist %> - SongElite</title>
    <link rel="icon" type="image/jpeg" href="/images/SongElite-icon.jpg" />
    <!-- Add Google Fonts -->
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&family=Poppins:wght@400;600&display=swap"
      rel="stylesheet"
    />

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <nav
      id="navbar"
      class="navbar navbar-expand-lg navbar-light bg-light fixed-top"
    >
      <div class="container">
        <img
          src="/images/SongElite-icon.jpg"
          alt="SongElite Logo"
          style="
            width: 40px;
            height: auto;
            border-radius: 50%;
            padding-right: 10px;
          "
        />

        <a class="navbar-brand" href="/">SongElite</a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarNav"
          aria-controls="navbarNav"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
          </ul>
        </div>
      </div>
    </nav>

    <header class="bg-dark text-white py-3">
      <div class="container text-center header-container">
        <h1>Now Playing: <%= song %> by <%= artist %></h1>
        <p class="tagline">
          Enjoy the best of <%= genre.charAt(0).toUpperCase() +
          genre.slice(1).toLowerCase() %> music
        </p>
      </div>
    </header>

    <main class="container text-center py-3">
      <div class="row">
        <div class="col-12">
          <img
            src="<%= image %>"
            alt="<%= genre %> Image"
            class="genre-image"
            id="toggleImage"
            style="cursor: pointer"
          />
        </div>
      </div>

      <div class="audio-player mt-4">
        <!-- Audio Controls -->
        <audio id="audio" controls autoplay>
          <source src="<%= songPath %>" type="audio/mp3" />
          Your browser does not support the audio element.
        </audio>
      </div>
    </main>

    <footer class="bg-dark text-white text-center py-1">
      <p>SongElite © 2024. All Rights Reserved.</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.js"></script>
    <script>
      document
        .getElementById("toggleImage")
        .addEventListener("click", function () {
          var audio = document.getElementById("audio");
          if (audio.paused) {
            audio.play();
          } else {
            audio.pause();
          }
        });
    </script>
  </body>
</html>
